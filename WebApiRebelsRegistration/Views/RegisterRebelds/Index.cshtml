@model IEnumerable<WebApiRebelsRegistration.Models.RegisterRebelds>

@{
    ViewBag.Title = "Index";
}

<h2>Registro de Rebeldes</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DateRegsiter)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Planets.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Rebelds.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DateRegsiter)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Planets.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Rebelds.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
